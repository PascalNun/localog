# Synthetic fixtures only

This directory is reserved for small, redistributable fixtures created specifically for LocaLog tests and demonstrations.

Never add real meeting audio, client or project names, transcripts, protocols, user paths, model files, exports, databases, logs, secrets, or diagnostic bundles.
